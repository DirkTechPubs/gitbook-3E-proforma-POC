### **Allow / Show Options (True, False)**

Some system options are for fields that are not editable (and not global). These use either Allow or Show in the naming convention and their available values are True or False. Except for the Show_WIPList option mentioned previously, all options have the system default of True.

